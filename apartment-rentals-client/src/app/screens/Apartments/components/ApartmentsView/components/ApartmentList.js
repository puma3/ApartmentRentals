import React from "react";

const ApartmentsList = ({ showMap }) => {
  return (
    <div>This is the List view. We show maps: {showMap ? "true" : "false"}</div>
  );
};

export default ApartmentsList;
